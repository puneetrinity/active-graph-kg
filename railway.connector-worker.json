{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "/activekg/connectors/**",
      "/activekg/graph/repository.py",
      "/activekg/common/**",
      "/requirements.txt",
      "/Dockerfile"
    ]
  },
  "deploy": {
    "startCommand": "python -m activekg.connectors.worker",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
